mixin setPicture(className,folder,name,ext,alt)
	picture(class=`${className}`)
		source(srcset=`/dist/webp-img/${folder}/${name}.webp` type="image/webp")
		img(src=`/dist/min-img/${folder}/${name}.${ext}` alt=`${alt}`)
